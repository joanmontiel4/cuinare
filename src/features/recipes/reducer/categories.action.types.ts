export enum actionTypes {
    'categories@load' = 'categories@load',
    'categories@add' = 'categories@add',
    'categories@update' = 'categories@update',
    'categories@delete' = 'categories@delete',
}
